let frog;

function preload() {
  frog = loadImage('assets/frog.gif')
};

function setup() {
  createCanvas(windowWidth, windowHeight);
  let x = auto(width);
  let y = auto(length);
};

Show(); {
image(frog, this.x, this.y);
};